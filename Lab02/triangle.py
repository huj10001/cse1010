base = input('Enter base of the triangle: ')
height = input('Enter the height of the triangle: ')
area = (1/2) * float(base) * float(height)
print('The area of a rectangle with base', base, 'and height', height, 'is', area)
