import math
radius = input('Enter radius of the cone: ')
height = input('Enter height of the cone: ')
area = math.pi * (float(radius)**2) * (float(height)/3)
print('The area of a sphere with radius', radius, 'and height', height, 'is', area)
