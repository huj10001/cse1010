import math
width = input('Enter width of the box: ')
height = input('Enter height of the box: ')
depth = input('Enter depth of the box: ')
area = float(width) * float(height) * float(depth)
print('The area of a box with width', width, ', height', height, 'and depth, depth', 'is', area)
